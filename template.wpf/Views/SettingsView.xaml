<UserControl x:Class="template.wpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid Margin="32">
        <Border Background="{StaticResource CardBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrushColor}" 
                BorderThickness="1" 
                CornerRadius="12" 
                Padding="32" 
                MaxWidth="480" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Left">
            <StackPanel>
                <!-- Header Badge -->
                <Border Background="#F1F5F9" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,12">
                    <TextBlock Text="SYSTEM CONFIGURATION" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="Bold"/>
                </Border>

                <TextBlock Text="Cấu hình hệ thống" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBlock Text="Tuỳ chỉnh các tham số Navigation Service và theme." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,24"/>

                <!-- Setting Item 1 -->
                <Border BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="0,0,0,1" Padding="0,0,0,16" Margin="0,0,0,16">
                    <DockPanel LastChildFill="False">
                        <StackPanel DockPanel.Dock="Left">
                            <TextBlock Text="Dark Mode" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="Chuyển đổi giao diện sáng/tối" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <CheckBox DockPanel.Dock="Right" IsChecked="{Binding DarkMode}" VerticalAlignment="Center"/>
                    </DockPanel>
                </Border>

                <!-- Setting Item 2 -->
                <DockPanel LastChildFill="False">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="LRU Cache Limit" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Số lượng View tối đa được cache trên RAM" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                    <TextBox DockPanel.Dock="Right" Text="{Binding CacheLimit}" Width="60" TextAlignment="Center" VerticalAlignment="Center"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
