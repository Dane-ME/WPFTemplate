<Window x:Class="template.wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:template.wpf.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="WPF Clean MVVM Navigator" Height="580" Width="960"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif"
        Background="{StaticResource SurfaceBackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBackgroundBrush}" Padding="16,24">
            <DockPanel>
                <!-- Brand / Logo Area -->
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,32">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="32" Height="32" Background="{StaticResource PrimaryBrush}" CornerRadius="8" Margin="0,0,10,0">
                            <TextBlock Text="⚡" Foreground="White" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="WPF CleanNav" Foreground="White" FontWeight="Bold" FontSize="15"/>
                            <TextBlock Text="Architecture Core" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Footer / Status Info -->
                <Border DockPanel.Dock="Bottom" Background="#1E293B" CornerRadius="8" Padding="12,10">
                    <TextBlock Text="Ready • v1.0.0" Foreground="#94A3B8" FontSize="11" TextAlignment="Center"/>
                </Border>

                <!-- Navigation Links -->
                <StackPanel VerticalAlignment="Top">
                    <TextBlock Text="MENU" Foreground="#64748B" FontSize="11" FontWeight="Bold" Margin="8,0,0,12"/>
                    <Button Content="🏠  Home" Command="{Binding NavigateHomeCommand}" Style="{StaticResource NavButtonStyle}" HorizontalContentAlignment="Left" Margin="0,0,0,4"/>
                    <Button Content="⚙️  Settings" Command="{Binding NavigateSettingsCommand}" Style="{StaticResource NavButtonStyle}" HorizontalContentAlignment="Left" Margin="0,0,0,4"/>
                    <Separator Background="#1E293B" Margin="0,12"/>
                    <Button Content="↩  Quay lại (Back)" Command="{Binding GoBackCommand}" Style="{StaticResource NavButtonStyle}" HorizontalContentAlignment="Left"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="56"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header Bar -->
            <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="0,0,0,1" Padding="24,0">
                <Grid VerticalAlignment="Center">
                    <TextBlock Text="Dashboard" FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- View Container -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
        </Grid>
    </Grid>
</Window>
