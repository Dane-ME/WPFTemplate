<UserControl x:Class="template.wpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid Margin="32">
        <Border Background="{StaticResource CardBackgroundBrush}" 
                BorderBrush="{StaticResource BorderBrushColor}" 
                BorderThickness="1" 
                CornerRadius="12" 
                Padding="32" 
                MaxWidth="480" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Left">
            <StackPanel>
                <!-- Header Badge -->
                <Border Background="#EFF6FF" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,12">
                    <TextBlock Text="AUTHENTICATION DEMO" Foreground="{StaticResource PrimaryBrush}" FontSize="11" FontWeight="Bold"/>
                </Border>

                <TextBlock Text="Đăng nhập tài khoản" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBlock Text="Thực thi Use Case từ tầng Application qua DI container." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,24"/>

                <!-- Input Field -->
                <TextBlock Text="Tên đăng nhập (Username)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                <!-- Action Button -->
                <Button Content="🚀  Thực thi UseCase" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="40" Margin="0,0,0,16"/>

                <!-- Status Feedback -->
                <Border Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="6" Padding="12,10">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#15803D" FontSize="13" FontWeight="Medium" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
